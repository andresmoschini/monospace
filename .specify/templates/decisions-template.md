# Decisions: [FEATURE NAME]

**Feature**: `[NNN-slug]` | **Written**: [DATE] | **Answered**: [DATE or _pending_]

<!--
  Written by /speckit-plan part one, together with research.md, and nothing else is written until
  this file is answered. Constitution: _The plan runs in two parts_ and _The decision sheet_.

  Rules, and they are what make this the one artifact read in full:

  - At most 7 entries, at most 60 lines of prose including this header. Render blocks do not count:
    they are what replaces prose, not what is added to it. More entries means the feature is too
    thick; say so and propose the split rather than shrinking the entries.
  - One line per field. If a field needs two, it is research, and research.md owns it.
  - Every `domain` entry is the maintainer's to answer.
  - Where the subject renders, SHOW both options and drop the prose line describing them
    (constitution, principle IV, _Show the rendering_). A picture is what makes an entry answerable
    in seconds without opening research.md. Where it does not render, "The alternative" carries it
    in one line. Either way, an entry with no real alternative was not a decision and does not
    belong here.
  - Nothing is decided outside this file. If implementation meets a decision the sheet does not
    hold, work stops and asks.
-->

## D1 — {the question, in one line}

<!-- render: A=examples/d1-proposal.json B=examples/d1-alternative.json -->

```text
A: {the proposal}    B: {the alternative}
{generated}          {generated}
```

<!-- /render -->

- **Proposal**: {one sentence — why, not what the picture already shows}
- **Altitude**: domain | module:{path} | tooling
- **If this is wrong**: {what reversing it costs, in one line}
- **Yours to answer**: yes | no — {why, where it is not obvious}
- **Answer**: _pending_

## D2 — {a subject that does not render}

- **Proposal**:
- **Altitude**:
- **If this is wrong**:
- **The alternative**:
- **Yours to answer**:
- **Answer**: _pending_
